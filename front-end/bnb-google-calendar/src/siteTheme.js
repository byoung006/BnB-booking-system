import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    main: "#465180",
  },
  success: {
    main: "#038f0f",
  },
  error: {
    main: "#ff0000",
  },
});
export default theme;
